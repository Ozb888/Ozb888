public boolean sum28(int[] numbers) {
boolean result =false;
int sum = 0;
  for (int i = 0; i <numbers.length;i=i+1)
  {
  if (numbers[i] == 2) sum+=2;
  }
  if (sum == 8) result = true;

  return result;
}
